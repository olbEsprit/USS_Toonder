import { Time } from "@angular/common";

export class Person {
  constructor(
    id?: string,
    firstName?: string,
    lastName?: string,
    gender?: string,
    location?: string,
    birthday?: string,
    pictureUrl?: string,
    age?: number
  ) { }
}  
